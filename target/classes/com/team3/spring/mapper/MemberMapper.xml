<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team3.spring.mapper.MemberMapper">
	<select id="login" resultType="com.team3.spring.vo.MemberVO">
		SELECT * FROM MEMBER WHERE ACCOUNT = #{id} AND PWD = #{pw}
	</select>
	<insert id="signup">
		INSERT INTO MEMBER (account, name, pwd, email1, email2, phonenumber, age)
		VALUES (#{account}, #{name}, #{pwd}, #{email1}, #{email2}, #{phonenumber}, #{age})
	</insert>
	<select id="checkAccount" resultType="int">
		SELECT COUNT(*) FROM MEMBER WHERE ACCOUNT = #{account}
	</select>
	<select id="checkEmail" resultType="int">
		SELECT COUNT(*) FROM MEMBER WHERE EMAIL1 = #{email1} AND EMAIL2 = #{email2}
	</select>
	<update id="userInfoModify">
		UPDATE MEMBER SET NAME = #{name}, EMAIL1 = #{email1}, EMAIL2 = #{email2}, PHONENUMBER = #{phonenumber}, AGE = #{age}
		WHERE ID = #{id}
	</update>
	<update id="userPwdModify">
		UPDATE MEMBER SET PWD = #{pwd}
		WHERE ID = #{id}
	</update>
	<delete id="userDelete">
		DELETE FROM MEMBER 
		WHERE ID = #{id}
	</delete>
</mapper>